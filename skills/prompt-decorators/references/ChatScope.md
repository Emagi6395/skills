# `+++ChatScope`

**Category:** Session Management

Switch all decorators in the current message to chat scope. They auto-apply to every
subsequent prompt until cleared. Any decorator specified in the same message as
`+++ChatScope` immediately becomes active at the chat level.
