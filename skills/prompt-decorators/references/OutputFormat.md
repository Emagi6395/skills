# `+++OutputFormat(format=FORMAT)`

**Category:** Output Control

Structure the entire response in the specified format without deviation.

**Parameter:** `format` — `json | markdown | yaml | xml` or any other named format.
