# `+++Export(format=FORMAT)`

**Category:** Export

Produce a concise, self-contained export or summary of the conversation.

**Structure:** `[Scope] → [Format] → [Exported Content]`

**Parameter:** `format` — `text | markdown | json | yaml`

Include any currently active decorators in the export if available.
