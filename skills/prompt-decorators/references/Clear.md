# `+++Clear`

**Category:** Session Management

Clear all active chat-scoped decorators.

Optionally, clear only specific decorators by naming them:
- `+++Clear` → clears all active decorators
- `+++Clear(+++Reasoning, +++Tone)` → clears only the named decorators
